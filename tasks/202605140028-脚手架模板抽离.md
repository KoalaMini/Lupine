# 任务跟踪 — 脚手架模板抽离与目录结构重组

## 任务概览

- **对应 Specs**: `specs/脚手架模板抽离-v1.0-202605140028.md`
- **对应 Plan**: `plans/202605140028-脚手架模板抽离.md`
- **执行日期**: 2026-05-14

---

## 已完成的任务

### Task 1: 创建 assets/ 目录和模板文件 ✅

从 `bin/lupine-init` 的 heredoc 中提取所有模板内容，创建独立静态文件：

| 文件 | 状态 |
|------|------|
| `assets/CLAUDE.md` | ✅ 已创建（移除 `.lupine/` 引用） |
| `assets/VISION.md` | ✅ 已创建 |
| `assets/README.md` | ✅ 已创建（新增） |
| `assets/rules/agents.md` | ✅ 已创建 |
| `assets/rules/coding.md` | ✅ 已创建 |
| `assets/rules/evals.md` | ✅ 已创建 |
| `assets/rules/git.md` | ✅ 已创建 |
| `assets/specs/README.md` | ✅ 已创建 |
| `assets/plans/README.md` | ✅ 已创建 |
| `assets/reviews/README.md` | ✅ 已创建 |
| `assets/tasks/README.md` | ✅ 已创建 |

### Task 2: 迁移 .lupine/ 内容到根目录 ✅

| 原路径 | 新路径 | 状态 |
|--------|--------|------|
| `.lupine/VISION.md` | `VISION.md` | ✅ 已覆盖 |
| `.lupine/specs/*` | `specs/` | ✅ 已复制 |
| `.lupine/plans/*` | `plans/` | ✅ 已复制 |
| `.lupine/reviews/README.md` | `reviews/README.md` | ✅ 已覆盖 |
| `.lupine/tasks/README.md` | `tasks/README.md` | ✅ 已覆盖 |

**修复**: 迁移后修复了 README.md 中的旧标题（`# .lupine/specs/` → `# specs/` 等）。

### Task 3: 重写 bin/lupine-init 脚本 ✅

- 删除所有 heredoc 硬编码模板
- 改为从 `assets/` 目录递归复制
- 保留 skill 安装逻辑（`scripts/install-skills.sh`）
- 保留输出提示和文件列表
- 新增 `assets/` 存在性检查，缺失时报错退出

### Task 4: 更新 CLAUDE.md 和 README.md ✅

- **CLAUDE.md**: 移除 `.lupine/` 相关说明；更新文件导航加入 `assets/` 和 `bin/lupine-init`
- **README.md**: 更新文件结构图加入 `assets/` 和 `bin/`；修复过时的 `AGENTS.md`/`EVALS.md` 引用

### Task 5: 删除 .lupine/ 目录并验证 ✅

- `.lupine/` 目录已完全删除
- 验证 `lupine-init` 运行成功，输出正确的文件结构
- 验证 `grep -r "\.lupine"` 无残留（排除 specs/plans/reviews 等本次文档的正常引用）

---

## 执行过程中的决策

| 决策点 | 选择 | 原因 |
|--------|------|------|
| `assets/README.md` 内容 | 新增基础模板 | 审查报告建议，Specs §3 要求 |
| 迁移后 README.md 标题 | 修复为 `# specs/` 等 | 避免残留 `.lupine/` 路径 |
| `lupine-init` 的 `cp` 行为 | 默认覆盖 | 与用户确认的覆盖式更新策略一致 |

---

## 验证结果

```bash
# 1. 初始化测试
./bin/lupine-init test-project /tmp/lupine-test
# ✅ 成功，文件结构完整（11 个文件）

# 2. assets 完整性
find assets -type f | wc -l
# ✅ 11 个模板文件

# 3. .lupine 删除确认
ls .lupine 2>/dev/null
# ✅ 目录不存在

# 4. 残留检查
grep -r "\.lupine" CLAUDE.md README.md rules/ assets/ bin/ 2>/dev/null
# ✅ 无残留
```

---

## 下一步

代码已完成，提交 **评估器②** 审查。
