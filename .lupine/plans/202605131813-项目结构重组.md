# 项目结构重组 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将项目重组为三层入口结构（根目录入口 + rules/规范仓库 + .lupine/自管理），统一小写命名。

**Architecture:** 通过文件移动、重命名和内容拆分，将 CLAUDE.md 的职责分散到轻量索引和 rules/ 规范文件中，同时建立 .lupine/ 作为框架自身迭代的独立空间。

**Tech Stack:** Markdown, Bash

---

## File Structure

| 文件 | 操作 | 说明 |
|------|------|------|
| `rules/git.md` | 新建 | 从 `CLAUDE.md` 提取 Git 规范 |
| `rules/coding.md` | 新建 | 从 `CLAUDE.md` 提取技术栈和编码约定 |
| `rules/agents.md` | 移动+修改 | 从 `AGENTS.md` 移动，更新内部路径引用 |
| `rules/evals.md` | 移动 | 从 `EVALS.md` 移动 |
| `CLAUDE.md` | 重写 | 从项目宪法变为轻量索引 |
| `PLANS/` | 重命名 | → `plans/` |
| `REVIEWS/` | 重命名 | → `reviews/` |
| `TASKS/` | 重命名 | → `tasks/` |
| `.lupine/specs/` | 新建目录 | 框架自身需求规格 |
| `.lupine/plans/` | 新建目录 | 框架自身技术设计 |
| `.lupine/reviews/` | 新建目录 | 框架自身审查报告 |
| `.lupine/tasks/` | 新建目录 | 框架自身任务跟踪 |
| `README.md` | 修改 | 更新文件结构说明 |
| `bin/lupine-init` | 修改 | 更新生成的目录名和模板内容 |

---

### Task 1: 创建 rules/ 目录和 git.md

**Files:**
- Create: `rules/git.md`

- [ ] **Step 1: 创建 rules/ 目录**

```bash
mkdir -p rules
```

- [ ] **Step 2: 写入 git.md**

```bash
cat > rules/git.md << 'EOF'
# Git 协作规范

## 分支策略
- `main`: 保护分支，仅通过 PR 合并
- `feat/{feature}`: 从 main 拉出，开发完成后 PR → main
- `fix/{bug}`: 紧急修复分支

## Commit 规范（Linux 社区风格）
- 格式: `{type}({scope}): {description}`
- type: feat / fix / docs / refactor / test / chore
- scope: 模块名，如 auth / user / db
- 正文说明改动原因（why），而非内容（what）
- 尾部附 AI 辅助信息：

  ```
  AI: {model-name}
  ```

- 示例:

  ```
  feat(auth): add JWT refresh token rotation

  原 refresh token 永不过期，改为轮换机制：
  每次刷新颁发新 refresh token，旧 token 作废。

  AI: kimi-k2.6
  ```

## PR 流程
- PR 必须经人工审核后方可合并
- AI 评估器的审查结果作为参考，不替代人工审核
- 禁止 self-merge
EOF
```

- [ ] **Step 3: 验证文件内容**

```bash
head -5 rules/git.md && echo "..." && tail -5 rules/git.md
```

Expected: 显示 "# Git 协作规范" 开头和 "禁止 self-merge" 结尾。

---

### Task 2: 创建 rules/coding.md

**Files:**
- Create: `rules/coding.md`

- [ ] **Step 1: 写入 coding.md**

```bash
cat > rules/coding.md << 'EOF'
# 技术栈与编码规范

## 技术栈
- 语言: Markdown
- 版本管理: Git
- 项目管理: GitHub Issues + PR

## 文件约定
- `specs/` — 需求规格说明书（.md）
- `plans/` — 技术设计方案（.md）
- `reviews/` — 审查报告（.md）
- `tasks/` — 任务跟踪（.md）

## 命名规范
- 文件夹统一小写：`specs/`, `plans/`, `reviews/`, `tasks/`
- 文件命名：`{功能名称}-v{版本号}-{YYYYMMDD}.md`
EOF
```

- [ ] **Step 2: 验证文件内容**

```bash
cat rules/coding.md
```

Expected: 包含技术栈、文件约定、命名规范三节。

---

### Task 3: 移动 AGENTS.md 到 rules/agents.md 并更新路径引用

**Files:**
- Delete: `AGENTS.md`
- Create: `rules/agents.md`

- [ ] **Step 1: 写入 agents.md（更新所有大写路径为小写）**

```bash
cat > rules/agents.md << 'EOF'
# Lupine Agent 集群配置

## 角色定义

| 角色 | 职责 | 方式 | 产出物 | 需用户参与 |
|------|------|------|--------|-----------|
| 分析器 Analyzer | 与用户对话、澄清产品愿景与需求、写需规 | 迭代式 | VISION.md + specs/{功能名称}-v{版本号}-{YYYYMMDD}.md | ✅ 每轮确认 |
| 规划器 Planner | 基于确认的需规做技术设计 | 一次性 | plans/YYYYMMDD-{功能名称}.md | ❌ |
| 评估器 Evaluator | 在两个门禁节点做审查 | 一次性 | reviews/{功能名称}-v{版本号}-{YYYYMMDD}-{类型}.md | ❌ |
| 执行器 Executor | 拆 task、写代码、写测试 | 拆并行 | 代码 + 测试 + tasks/YYYYMMDD-{功能名称}.md | ❌ |

## 分析器工作流程

### 步骤一：产品定位与愿景（项目级，仅需一次）

若项目尚无 `VISION.md`，分析器需先与用户探讨：

1. 目标用户画像（为谁解决什么问题）
2. 核心价值主张（一句话描述）
3. 成功标准（如何衡量成功）
4. 范围边界（明确不做的事项）

产出：`VISION.md`（项目根目录，人工确认后锁定）

### 步骤二：功能级需求澄清（每个功能一次）

1. 读取 `VISION.md`、项目已有上下文（`CLAUDE.md`、已有 `specs/`）
2. 向用户提出 3-5 个结构化问题，覆盖：
   - 功能范围（做什么、不做什么）
   - 安全与约束（密码规则、权限、合规）
   - 边界情况（错误处理、上限、异常场景）
   - 非功能要求（性能、可扩展性、部署方式）
3. 根据用户回答，草拟 `specs/{功能名称}-v{版本号}-{YYYYMMDD}.md`
4. 展示给用户确认
5. 用户提出修改意见 → 迭代 specs → 再确认
6. 用户说"可以了" → 锁定版本，结束

**迭代终止条件**：用户明确确认当前版本。

## 工作流规则

1. 分析器和规划器不可同时进行——需求必须先确认才能做设计
2. 评估器（code）未通过 → 打回执行器，附审查报告作为上下文
3. 评估器（plan）未通过 → 打回规划器，附审查报告作为上下文
4. 评估器（plan）发现需规遗漏 → 打回分析器，而非让规划器补
5. **不允许跳过评估阶段直接合并**
6. **PR 必须经人工审核后方可合并**（AI 评估器审查结果供参考，不替代人工审核）
7. 禁止 self-merge

## 例外处理

| 场景 | 处理方式 |
|------|---------|
| 需求变更 | 回到分析器，更新 specs，标记新版本号 |
| 实现中发现 plan 不可行 | 回到规划器修 plan，再经评估器（plan）审查 |
| 紧急修复（如线上 bug） | 跳过调研+规划，直接执行+评估②，事后补 specs |
| 微小变更（如改文案） | 跳过调研和规划，直接执行后评估② |
EOF
```

- [ ] **Step 2: 删除原 AGENTS.md**

```bash
rm AGENTS.md
```

- [ ] **Step 3: 验证**

```bash
ls rules/agents.md && test ! -f AGENTS.md && echo "OK"
```

Expected: 输出 `rules/agents.md` 和 `OK`。

---

### Task 4: 移动 EVALS.md 到 rules/evals.md

**Files:**
- Delete: `EVALS.md`
- Create: `rules/evals.md`

- [ ] **Step 1: 写入 evals.md（内容不变）**

```bash
cat > rules/evals.md << 'EOF'
# Lupine 评估标准

## 安全门禁（阻断）
- [ ] 密码/密钥/token 绝不硬编码
- [ ] 敏感信息不出现在日志或响应中
- [ ] SQL 注入防护
- [ ] 认证中间件覆盖所有需鉴权的路由

## 质量门禁（阻断）
- [ ] 新增代码测试覆盖率 ≥ 80%
- [ ] 圈复杂度 ≤ 15
- [ ] 不引入新的 lint error

## 架构门禁（警告）
- [ ] handler 层不含业务逻辑
- [ ] service 层不含 HTTP 细节
- [ ] 依赖方向是否正确
EOF
```

- [ ] **Step 2: 删除原 EVALS.md**

```bash
rm EVALS.md
```

- [ ] **Step 3: 验证**

```bash
ls rules/evals.md && test ! -f EVALS.md && echo "OK"
```

Expected: 输出 `rules/evals.md` 和 `OK`。

---

### Task 5: 重写 CLAUDE.md 为轻量索引

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: 重写 CLAUDE.md**

```bash
cat > CLAUDE.md << 'EOF'
# Lupine 项目

> 多 Agent 协作开发流水线。AI session 的第一入口。

## AI 阅读顺序

1. **本项目总纲** → `VISION.md`
2. **角色定义与工作流** → `rules/agents.md`
3. **评估门禁标准** → `rules/evals.md`
4. **Git 协作规范** → `rules/git.md`
5. **技术栈与编码规范** → `rules/coding.md`

## 当前角色指引

- 若用户要求"分析需求"或"调研" → 你是**分析器 Analyzer**
- 若用户要求"设计"或"/plan" → 你是**规划器 Planner**
- 若用户要求"审查"或"review" → 你是**评估器 Evaluator**
- 若用户要求"实现"或"写代码" → 你是**执行器 Executor**

各角色的详细职责见 `rules/agents.md`。

## 文件导航

| 文件/目录 | 用途 |
|-----------|------|
| `CLAUDE.md` | 本文件：AI 索引入口 |
| `README.md` | 项目介绍（面向人） |
| `VISION.md` | 产品定位与愿景（总纲） |
| `rules/git.md` | Git 分支、Commit、PR 规范 |
| `rules/coding.md` | 技术栈、编码约定、命名规范 |
| `rules/agents.md` | 四角色定义与工作流规则 |
| `rules/evals.md` | 评估门禁 checklist |
| `specs/` | 功能级需求规格说明书 |
| `plans/` | 技术设计方案 |
| `reviews/` | 审查报告 |
| `tasks/` | 任务跟踪 |
EOF
```

- [ ] **Step 2: 验证行数 ≤ 50**

```bash
wc -l < CLAUDE.md
```

Expected: 输出数字应 ≤ 50。

---

### Task 6: 重命名 PLANS/ → plans/

**Files:**
- Rename: `PLANS/` → `plans/`

- [ ] **Step 1: 重命名目录**

```bash
git mv PLANS plans
```

- [ ] **Step 2: 验证**

```bash
ls -d plans && test ! -d PLANS && echo "OK"
```

Expected: 输出 `plans` 和 `OK`。

---

### Task 7: 重命名 REVIEWS/ → reviews/

**Files:**
- Rename: `REVIEWS/` → `reviews/`

- [ ] **Step 1: 重命名目录**

```bash
git mv REVIEWS reviews
```

- [ ] **Step 2: 验证**

```bash
ls -d reviews && test ! -d REVIEWS && echo "OK"
```

Expected: 输出 `reviews` 和 `OK`。

---

### Task 8: 重命名 TASKS/ → tasks/

**Files:**
- Rename: `TASKS/` → `tasks/`

- [ ] **Step 1: 重命名目录**

```bash
git mv TASKS tasks
```

- [ ] **Step 2: 验证**

```bash
ls -d tasks && test ! -d TASKS && echo "OK"
```

Expected: 输出 `tasks` 和 `OK`。

---

### Task 9: 创建 .lupine/ 目录结构

**Files:**
- Create directories: `.lupine/specs/`, `.lupine/plans/`, `.lupine/reviews/`, `.lupine/tasks/`

- [ ] **Step 1: 创建目录和 README**

```bash
mkdir -p .lupine/specs .lupine/plans .lupine/reviews .lupine/tasks

cat > .lupine/specs/README.md << 'EOF'
# .lupine/specs/

Lupine 框架自身的需求规格说明书。

命名规范：`{功能名称}-v{版本号}-{YYYYMMDD}.md`
EOF

cat > .lupine/plans/README.md << 'EOF'
# .lupine/plans/

Lupine 框架自身的技术设计方案。

命名规范：`YYYYMMDD-{功能名称}.md`
EOF

cat > .lupine/reviews/README.md << 'EOF'
# .lupine/reviews/

Lupine 框架自身的审查报告。

命名规范：`{功能名称}-v{版本号}-{YYYYMMDD}-{类型}.md`
EOF

cat > .lupine/tasks/README.md << 'EOF'
# .lupine/tasks/

Lupine 框架自身的任务跟踪。

命名规范：`YYYYMMDD-{功能名称}.md`
EOF
```

- [ ] **Step 2: 验证结构**

```bash
find .lupine -type f | sort
```

Expected:
```
.lupine/plans/README.md
.lupine/reviews/README.md
.lupine/specs/README.md
.lupine/tasks/README.md
```

---

### Task 10: 更新 README.md 文件结构说明

**Files:**
- Modify: `README.md:62-79`

- [ ] **Step 1: 替换文件结构区块**

找到 `README.md` 中的旧文件结构代码块（约在 62-79 行），替换为：

```markdown
## File Structure · 文件结构

```
project-root/
├── CLAUDE.md          # AI 索引入口（轻量导航）
├── README.md          # 项目介绍（面向人）
├── VISION.md          # 产品定位与愿景（分析器产出，项目级）
├── rules/
│   ├── git.md         # Git 协作规范
│   ├── coding.md      # 技术栈与编码规范
│   ├── agents.md      # 四角色定义 + 工作流规则
│   └── evals.md       # 评估门禁标准 checklist
├── specs/             # 功能级需求规格说明书（分析器产出）
│   └── {功能名称}-v{版本号}-{YYYYMMDD}.md
├── plans/             # 技术设计方案（规划器产出）
│   └── YYYYMMDD-{功能名称}.md
├── reviews/           # 审查报告（评估器产出）
│   ├── {功能名称}-v{版本号}-{YYYYMMDD}-plan.md
│   └── {功能名称}-v{版本号}-{YYYYMMDD}-code.md
└── tasks/             # 任务跟踪（执行器产出）
    └── YYYYMMDD-{功能名称}.md
```

- **CLAUDE.md**：AI 的第一上下文，所有 session 自动读取
- **VISION.md**：分析器产出的产品总纲，所有功能需求的锚点
- **rules/**：所有规范类文件聚合处
```

- [ ] **Step 2: 验证**

```bash
grep -A 20 "File Structure" README.md | head -25
```

Expected: 显示新的文件结构，包含 `rules/` 和小写的 `specs/`, `plans/`, `reviews/`, `tasks/`。

---

### Task 11: 更新 bin/lupine-init 初始化脚本

**Files:**
- Modify: `bin/lupine-init`

- [ ] **Step 1: 修改目录创建行（第 19 行）**

将：
```bash
mkdir -p SPECS PLANS REVIEWS TASKS
```
替换为：
```bash
mkdir -p specs plans reviews tasks rules
```

- [ ] **Step 2: 修改 CLAUDE.md 模板（第 21-66 行）**

将整段 `cat > CLAUDE.md << 'EOF'` 到对应 `EOF` 的内容替换为：

```bash
cat > CLAUDE.md << 'EOF'
# {项目名称}

> {一句话描述项目}

## AI 阅读顺序

1. **本项目总纲** → `VISION.md`
2. **角色定义与工作流** → `rules/agents.md`
3. **评估门禁标准** → `rules/evals.md`
4. **Git 协作规范** → `rules/git.md`
5. **技术栈与编码规范** → `rules/coding.md`

## 当前角色指引

- 若用户要求"分析需求"或"调研" → 你是**分析器 Analyzer**
- 若用户要求"设计"或"/plan" → 你是**规划器 Planner**
- 若用户要求"审查"或"review" → 你是**评估器 Evaluator**
- 若用户要求"实现"或"写代码" → 你是**执行器 Executor**

各角色的详细职责见 `rules/agents.md`。

## 文件导航

| 文件/目录 | 用途 |
|-----------|------|
| `CLAUDE.md` | 本文件：AI 索引入口 |
| `README.md` | 项目介绍（面向人） |
| `VISION.md` | 产品定位与愿景（总纲） |
| `rules/git.md` | Git 分支、Commit、PR 规范 |
| `rules/coding.md` | 技术栈、编码约定、命名规范 |
| `rules/agents.md` | 四角色定义与工作流规则 |
| `rules/evals.md` | 评估门禁 checklist |
| `specs/` | 功能级需求规格说明书 |
| `plans/` | 技术设计方案 |
| `reviews/` | 审查报告 |
| `tasks/` | 任务跟踪 |
EOF
```

- [ ] **Step 3: 修改 AGENTS.md 模板路径引用（第 68-126 行）**

将整段 `cat > AGENTS.md << 'EOF'` 替换为 `cat > rules/agents.md << 'EOF'`，并将模板内所有 `SPECS/` 替换为 `specs/`，`PLANS/` 替换为 `plans/`，`REVIEWS/` 替换为 `reviews/`，`TASKS/` 替换为 `tasks/`。

同时，将文件末尾的循环（第 186-195 行）中的 `SPECS PLANS REVIEWS TASKS` 替换为 `specs plans reviews tasks`。

- [ ] **Step 4: 修改 EVALS.md 输出路径（第 128-146 行）**

将 `cat > EVALS.md << 'EOF'` 替换为 `cat > rules/evals.md << 'EOF'`。

- [ ] **Step 5: 新增 rules/git.md 和 rules/coding.md 模板**

在 VISION.md 模板之后、`for dir in ...` 循环之前，插入：

```bash
cat > rules/git.md << 'EOF'
# Git 协作规范

## 分支策略
- `main`: 保护分支，仅通过 PR 合并
- `feat/{feature}`: 从 main 拉出，开发完成后 PR → main
- `fix/{bug}`: 紧急修复分支

## Commit 规范
- 格式: `{type}({scope}): {description}`
- type: feat / fix / docs / refactor / test / chore
- 正文说明改动原因（why），而非内容（what）
- 尾部附 AI 辅助信息：`AI: {model-name}`

## PR 流程
- PR 必须经人工审核后方可合并
- 禁止 self-merge
EOF

cat > rules/coding.md << 'EOF'
# 技术栈与编码规范

## 技术栈
- 语言/框架/数据库/基础设施
- 关键依赖版本

## 文件约定
- `specs/` — 需求规格说明书
- `plans/` — 技术设计方案
- `reviews/` — 审查报告
- `tasks/` — 任务跟踪

## 命名规范
- 文件夹统一小写
- 文件命名：`{功能名称}-v{版本号}-{YYYYMMDD}.md`
EOF
```

- [ ] **Step 6: 验证脚本语法**

```bash
bash -n bin/lupine-init && echo "Syntax OK"
```

Expected: 输出 `Syntax OK`。

---

## Self-Review

**1. Spec coverage:**
- [x] CLAUDE.md 转变为轻量索引 → Task 5
- [x] rules/ 目录新建，聚合所有规范 → Task 1-4
- [x] 命名统一小写 → Task 6-8
- [x] .lupine/ 目录结构新建 → Task 9
- [x] 更新所有内部引用 → Task 10-11

**2. Placeholder scan:**
- [x] 无 "TBD", "TODO", "implement later"
- [x] 所有代码块包含完整内容
- [x] 无 "Similar to Task N"

**3. Consistency check:**
- [x] `specs/`, `plans/`, `reviews/`, `tasks/` 在 agents.md、CLAUDE.md、README.md、lupine-init 中统一为小写
- [x] `rules/` 路径在所有任务中一致

---

## Execution Handoff

**Plan complete and saved to `plans/202605131813-项目结构重组.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
